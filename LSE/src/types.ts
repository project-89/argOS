export type ComponentMap = {
  [key: string]: any; // This should be more specific based on your component structure
};

export type GeneratedComponentMap = {
  [key: string]: string;
};

export type EntityMap = {
  [key: string]: number; // Maps entity names to their entity IDs
};

export type RelationMap = {
  [key: string]: any;
};
